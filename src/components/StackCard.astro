---
type Props = {
  text: string
  icon: string
}

const { text, icon } = Astro.props
---

<div class="w-fit px-3 py-2 group rounded border flex gap-2 items-center border-neutral-200 dark:border-neutral-700 hover:bg-neutral-100 hover:dark:bg-neutral-800 blend">
  <img src={`/icons/${icon}.svg`} alt={text} height={20} width={20} class="size-6" />
  <span class="text-sm capitalize text-neutral-500 dark:text-neutral-400 group-hover:text-black group-hover:dark:text-white blend">
    {text}
  </span>
</div>
